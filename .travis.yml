language: node_js

node_js: 12.14.0

git:
  quiet: true

cache:
  directories:
    - node_modules

branches:
  only:
    - /^dev\/.*$/
    - master

before_install:
  - sudo apt-get -y install sshpass

before_script: skip

script:
  - npm run build
  - ./config/deploy
